steps:
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      - functions
      - deploy
      - subscriber-pipeline
      - --runtime=python311
      - --trigger-http
      - --allow-unauthenticated
      - --entry-point=subscriber_pipeline
      - --source=.
      - --region=us-central1
      - --memory=512MB
      - --timeout=540s
      - --project=298391770247
      - --set-env-vars=GOOGLE_SHEET_NAME="Subscriber List",SMTP_SERVER=smtp.gmail.com,SMTP_PORT=587,FROM_NAME="Subscriber Pipeline"
      - --service-account=298391770247-compute@developer.gserviceaccount.com

  # Grant the Cloud Function access to Secret Manager
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      - projects
      - add-iam-policy-binding
      - '298391770247'
      - --member=serviceAccount:298391770247-compute@developer.gserviceaccount.com
      - --role=roles/secretmanager.secretAccessor

options:
  logging: CLOUD_LOGGING_ONLY
  machineType: 'E2_HIGHCPU_8'

# Ensure we have the right permissions
serviceAccount: 'projects/298391770247/serviceAccounts/298391770247-compute@developer.gserviceaccount.com'